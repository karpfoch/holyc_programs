/* every program must have a function 
    called main somewhere */
U0 Main()
{
    /* all variable must be declared 
        before they are used */
    U8 *message = "hello, world";

    /* statement that prints 
        without calling the function printf */
    "%s\n", message;
}
Main;
